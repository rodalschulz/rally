// Optional seed — keep empty for open signup; users appear when they Google-login.
import "dotenv/config";

async function main() {
  console.log("No seed data. Sign in with Google to create the first user.");
}

main();
